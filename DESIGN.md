# 南宫集团游戏导航网站 - 设计文档

## 1. 项目概述

**项目名称**：NG游戏导航平台  
**项目类型**：静态导航网站  
**核心功能**：游戏资讯导航、下载链接聚合、优惠活动展示  
**目标用户**：游戏玩家

---

## 2. 技术栈

### 前端
- **框架**：原生 HTML + JavaScript (ES6 Modules)
- **样式**：Tailwind CSS (CDN)
- **字体**：本地字体 (Sora, Hanken Grotesk, JetBrains Mono)
- **图标**：内联 SVG (Lucide 风格)

### 部署
- **服务器**：Nginx
- **CDN**：Cloudflare (可选)
- **域名**：支持自定义域名

---

## 3. 目录结构

```
/data/ngdh/
├── index.html              # 主页面
├── DESIGN.md               # 本文档
├── nginx.conf              # Nginx 配置 (防攻击版)
├── css/
│   ├── fonts.css           # 本地字体定义
│   └── styles.css          # 全局样式 (玻璃效果、鎏金主题)
├── js/
│   ├── app.js              # 主应用逻辑
│   ├── tailwind.config.js  # Tailwind 配置
│   ├── components/
│   │   └── NavigationCard.js
│   └── data/
│       └── nav-data.js
├── images/                 # 图片资源 (18个文件)
└── pages/                  # 子页面
    ├── promotion.html      # 注册送
    ├── first-deposit.html  # 首存100%赠送
    ├── download.html       # 下载地址
    ├── vip.html            # VIP专属接待
    └── warning.html        # 公示避坑
```

---

## 4. 页面结构

### 4.1 主页面 (index.html)

```
┌─────────────────────────────────────┐
│ 顶部通知栏 (滚动字幕)                │
├─────────────────────────────────────┤
│ 导航栏 (Logo + 标题 + 搜索框)        │
├─────────────────────────────────────┤
│                                     │
│ Hero 区域 (注册就送88-888 + 标签)   │
│                                     │
├─────────────────────────────────────┤
│ 导航卡片网格 (6列 → 4列响应式)       │
│ [游戏1][游戏2][游戏3][游戏4]...     │
├─────────────────────────────────────┤
│ 认证图片                            │
├─────────────────────────────────────┤
│ 热门指数图表                        │
├─────────────────────────────────────┤
│ 横幅卡片 (5个入口)                  │
├─────────────────────────────────────┤
│ 页脚 (执照信息 + 版权)              │
└─────────────────────────────────────┘
```

### 4.2 子页面

每个子页面包含：
- 顶部通知栏
- 导航栏（返回首页）
- 主内容区域
- 页脚

---

## 5. 功能特性

### 5.1 核心功能

| 功能 | 说明 |
|------|------|
| 动态倒计时 | 福利截止时间显示，每4小时重置 |
| 搜索功能 | 搜索游戏卡片 |
| 页面路由 | Hash 路由 (#/, #/search, #/download) |
| 响应式布局 | 适配手机/平板/桌面 |

### 5.2 视觉效果

| 效果 | 实现方式 |
|------|----------|
| 玻璃面板 | `backdrop-filter: blur()` + 半透明背景 |
| 鎏金标题 | `linear-gradient` + 文字描边 |
| 流动光效 | CSS `@keyframes` 动画 |
| 热门图表 | 柱状图显示游戏热度 |

---

## 6. 性能优化

### 6.1 外部请求统计

| 资源 | 数量 |
|------|------|
| Tailwind CSS CDN | 1 (6个页面共享) |
| 本地字体 (TTF) | 8 个文件 |
| 本地 CSS | 2 个文件 |

### 6.3 建议的后续优化

- 本地化 Tailwind CSS（生成独立 CSS 文件）
- 图片优化（压缩、WebP 格式）
- 启用 CDN 加速

---

## 7. 安全配置

### 7.1 Nginx 安全措施

| 配置 | 作用 |
|------|------|
| 安全响应头 | X-Frame-Options, X-XSS-Protection 等 |
| 请求限流 | 每IP每秒10次，突发20次 |
| 连接限制 | 每IP最多50个并发 |
| 恶意请求拦截 | SQL注入、XSS、路径遍历 |
| 隐藏文件保护 | 禁止访问 .git 等 |

### 7.2 建议的安全增强

- 部署 Cloudflare CDN
- 配置 HTTPS (Let's Encrypt)
- 安装 fail2ban
- 启用流量监控告警

---

## 8. 部署指南

### 8.1 服务器部署

```bash
# 1. 上传文件
scp -r /data/ngdh/* user@server:/var/www/ngdh/

# 2. 配置 Nginx
sudo cp nginx.conf /etc/nginx/sites-available/ngdh
sudo ln -s /etc/nginx/sites-available/ngdh /etc/nginx/sites-enabled/
sudo nginx -t

# 3. 重载 Nginx
sudo systemctl reload nginx
```

### 8.2 域名配置

1. 将域名解析到服务器 IP
2. 修改 nginx.conf 中的 `server_name`
3. 配置 HTTPS（推荐 Let's Encrypt）

---

## 9. 维护指南

### 9.1 更新内容

- **导航卡片**：编辑 `js/data/nav-data.js`
- **横幅链接**：编辑 `js/app.js` 中的 Banner Cards 部分
- **样式调整**：编辑 `css/styles.css`

### 9.2 添加新页面

1. 复制现有子页面
2. 修改内容和链接
3. 在 app.js 中添加路由

---

## 10. 版本信息

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0.0 | 2024-05-08 | 初始版本 |
| 1.1.0 | 2024-05-10 | 本地化字体、SVG图标、安全优化 |

---

*最后更新：2024-05-10*